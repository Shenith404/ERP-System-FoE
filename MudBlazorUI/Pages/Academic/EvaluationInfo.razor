@page "/ModuleManagementTabs/{ModuleOfferingId:guid}/{EvaluationId:guid}"
@using ERP_EvaluationManagement.ModuleManagementPages.Pages
@attribute [Authorize(Roles = "Admin,Staff,Lecture")]
<EvaluationManagementPage ModuleOfferingId="ModuleOfferingId" EvaluationId="EvaluationId" />

@code {
    [Parameter] 
    public Guid ModuleOfferingId { get; set; }

    [Parameter] 
    public Guid EvaluationId { get; set; }
}